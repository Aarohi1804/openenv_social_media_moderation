[project]
name = "social_media_moderation_env"
version = "0.1.0"
description = "Elite Trust & Safety Simulation for Meta OpenEnv Hackathon"
dependencies = [
    "openenv-core[core]>=0.2.2",
    "openai>=1.0.0",
    "pyyaml>=6.0",
    "fastapi>=0.104.0",
    "uvicorn[standard]>=0.24.0",
    "pydantic>=2.0.0",
    "httpx>=0.25.0", # Essential for the async EnvClient
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]